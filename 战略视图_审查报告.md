# 战略视图施工任务书审查报告

审查对象：`pipeline/docs/施工任务书_战略视图.md`  
审查日期：2026-08-20  
结论：**当前版本不宜直接派发施工。** 设计主方向可落地，且新建独立读模型、保留 `voc_spu_issue` 兼容读口、停止读取旧 `scope` 的总体边界与 v3 一致；但现有文字中有 9 处会导致运行失败、错误数据、误删另一类轴或正确实现反而过不了测试的阻断项。修正这些点不需要重做方案。

本次只做静态审查；未连接数据库、未执行 SQL、未调用 LLM/embedding、未联网、未使用 git。任务书 §9 的生产探针总量无法在这些边界下独立复测，因此不把未复测的生产数字列为错误；下面“事实错误”仅列代码、DDL、文档内证和数学可独立证明的项目。

## 一、任务书中独立核实为事实错误的地方

这是本报告最重要的一节。

### F1【阻断】两类成员详情链接都写错了现有路由

- 任务书把老品链接写成 `/{link_spu}/{opp_id}`，把新品链接写成 `/innovation/{opp_id}`：`pipeline/docs/施工任务书_战略视图.md:247-252`。
- 实际老品问题详情路由前缀是 `/issue`，完整路径为 `/issue/{spu}/{opp_id}`：`system/app/routes/issue.py:13-17`；现有模板也使用该路径：`system/app/templates/partials/issue_list.html:9-12`。
- 实际新品路由前缀是 `/inno`，完整路径为 `/inno/{opp_id}`：`system/app/routes/innovation.py:13,35-36`。

按任务书字面实现后，详情页上的所有成员下钻链接都会 404。应把两条路径分别改为 `/issue/{link_spu}/{opp_id}` 与 `/inno/{opp_id}`。

### F2【阻断】finalize 的实际编排点不在 `voc_analytics/pipeline.py`

- 任务书把交付物和挂钩位置写成 `pipeline/voc_analytics/pipeline.py`，并要求在那里增加 `--skip-strategy`：`pipeline/docs/施工任务书_战略视图.md:35,264-273`。
- 实际 `_finalize()`、命令行参数和成功退出逻辑都在 `pipeline/scripts/run_generate.py`：`pipeline/scripts/run_generate.py:89-212,215-233,236-281`。其中现有 `--skip-finalize` 也在该脚本的 argparse 中：`pipeline/scripts/run_generate.py:223-224`。
- `rerun_both.sh` 的唯一统一收尾同样调用 `run_generate.py --finalize-only`：`pipeline/scripts/rerun_both.sh:438-457`。

只改 `pipeline.py` 无法给现有入口增加 CLI 旗标，也无法保证统一收尾后触发战略重算。交付物清单必须加入并指定修改 `pipeline/scripts/run_generate.py`；若仍把实际重算函数放在 `pipeline.py`，也必须由 `run_generate.py` 显式调用。

### F3【阻断】§6 测试 8 的 90/10 期望值算错

任务书写“构造 90/10 分布，`n_eff≈1.25`”：`pipeline/docs/施工任务书_战略视图.md:289`。按任务书自己的公式：

`n_eff = (90+10)^2 / (90^2+10^2) = 10000/8200 = 1.219512...`

因此正确期望应约为 **1.22**，不是 1.25。若用常规 `pytest.approx`，正确实现会被错误测试拒绝。

### F4【阻断】034 被称为“可重跑”，但给出的 DDL 不是可重跑 DDL

- 任务书要求沿用 030–033 的“可重跑”防护：`pipeline/docs/施工任务书_战略视图.md:67-70`。
- 随后的三个表和索引均为无条件 `CREATE TABLE` / `CREATE INDEX`：同文件 `:71-112`。第二次执行会因对象已存在而失败。

任务书需要明确 034 的幂等策略，并让示例 DDL 与之相符。仅写“可重跑”不足以验收。

### F5【阻断】“切换对本模块透明”在 v3 shadow 并存期不成立

- 任务书的机会点输入只按类型、分类状态、合并状态和向量过滤，没有代次条件：`pipeline/docs/施工任务书_战略视图.md:120-129`；并且明确让不在兼容视图中的老品卡继续参与聚类：同文件 `:175-181`。
- v3 的既有契约是灰度期 `OPP-*` 与 `OPP2-*` 同表并存，任何按 `opp_type` 全表扫描的查询都必须显式限定代次：`pipeline/docs/施工任务书_v3.md:182-184`。
- 两个 `voc_spu_issue` 版本各自只含一代：v2 过滤 `OPP-*`，v3 过滤 `OPP2-*`：`pipeline/sql/032_spu_issue_v2v3.sql:43-78,80-110`。

所以兼容视图切到任一代时，任务书当前输入仍会把另一代机会点一起聚类；另一代老品卡只是权重变成 0，新品则还会直接重复进入。这里不是透明切换，而是混代产轴。必须冻结“战略层当前发布代次”的判定，并同时应用于两类机会点输入、证据计数和兼容视图。

### F6【建议】TOPK 的理论对数上界不是 `n*K/2`

任务书注释称每卡取 K 个近邻后，对数上界为 `n*K/2`：`pipeline/docs/施工任务书_战略视图.md:197-203`。归一化无向对并去重只能保证不超过 `min(n*K, n*(n-1)/2)`；只有每条近邻关系必然双向时才可再除以 2，而 top-K KNN 不保证互为近邻。

这不推翻实测约 4 千对或 `MAX_PAIRS=20000` 的起始值，但应修正注释，避免把保险丝余量按错误上界评估。

### F7【建议】交付物说“两个提示词”，正文实际定义了三个模板

- 交付物写 `prompts.py` 追加两个提示词：`pipeline/docs/施工任务书_战略视图.md:32`；§3.4 标题也写“两个”：同文件 `:150`。
- 正文实际要求 `STRATEGY_AXIS_SAME`、`STRATEGY_THEME_SAME`、`STRATEGY_AXIS_NAME` 三个模板：同文件 `:152-167`。

应改成“3 个模板：2 个判定模板 + 1 个共用命名模板”，避免施工清单与验收按不同数量计数。

### F8【建议】`prod_lines` “实测全空”与 §9 的数据事实、聚合规则矛盾

- 表定义注释说 `prod_lines` 实测全空：`pipeline/docs/施工任务书_战略视图.md:81`。
- 聚合规则要求对成员卡 `prod_line` 去重：同文件 `:185`。
- §9 又写新品 964/964 的值是字符串“未定”，不是 NULL/空：同文件 `:362-363`。

若照聚合规则实现，结果会是 `['未定']` 而不是 `{}`。应明确是否把“未定”当哨兵过滤；界面不展示该列不等于落库值为空。

### F9【建议】DDL 注释声称的“分区不变量”没有被数据库真正约束

任务书称唯一索引保证“同一类型下一张卡只能属于一个轴”：`pipeline/docs/施工任务书_战略视图.md:98-100`。但成员表只以 `axis_id` 外键关联父表：同文件 `:89-99`，没有约束成员的冗余 `axis_type` 必须等于父轴 `axis_type`。错误写入一个伪造的成员类型即可绕过所谓分区不变量。

可用组合外键绑定 `(axis_id, axis_type)`，或移除这份不受约束的冗余并直接对 `opp_id` 做符合真实业务的唯一约束。至少不要把现有 DDL 描述成已在数据库层保证该不变量。

### F10【存疑】“首页六区块计数”不是当前代码里的实际位置

- 任务书称要改“首页（queries.py 顶部六区块计数里的 strategy 子查询）”，并要求 `routes/home.py`、模板适配：`pipeline/docs/施工任务书_战略视图.md:254-262`。
- 当前 `strategy` 计数位于 `SHELL_COUNTS`：`system/app/queries.py:34-65`，由通用模板上下文 `shell_counts()` 读取：`system/app/web.py:31-37`，展示位置是侧栏：`system/app/templates/base.html:26-30,49-50`。
- 首页路由本身只查询 HOME2 的来源、流转、状态、新鲜度、周趋势和分布：`system/app/routes/home.py:24-35`，没有战略计数查询。

需由业主确认意图是“只改侧栏战略总数”，还是还要在首页新增展示。前者无需改 `routes/home.py`；后者目前缺具体位置和验收形态。

## 二、其余阻断项

### B1【阻断】034 缺少角色授权，管线和页面建完也读写不了

034 由 `voc_admin` 创建对象，却没有写任何 GRANT。现有应用固定以 human 身份连接：`system/app/db.py:1,26-39`；管线默认以 `voc_writer` 连接：`pipeline/voc_analytics/config.py:65-71`。032 对新增派生对象会显式授权三角色：`pipeline/sql/032_spu_issue_v2v3.sql:127-129`。

034 至少要写清：

- `voc_writer` 对轴、成员表拥有全量重建所需的 SELECT/INSERT/DELETE，对判定缓存拥有 SELECT/INSERT（以及实现确需的冲突更新权限）；
- `voc_human`、`voc_reader` 对轴和成员表拥有 SELECT；
- 判定缓存是否对 human/reader 暴露须明确，页面本身不需要它。

§6 还应加静态授权契约；否则路由 smoke 的 fake DB 会把生产权限错误完全遮住。

### B2【阻断】`--type` 与“DELETE 全部旧行”组合会误删另一类产物

- 独立入口支持 `--type {通病,诉求,all}`：`pipeline/docs/施工任务书_战略视图.md:264-268`。
- 落库却规定无条件 `DELETE` 全部旧轴后再插入本次结果：同文件 `:187-189`。

若运行 `--type 通病`，照字面会删除诉求主题且不会重建它；反之亦然。必须冻结为：单类型运行只替换该 `axis_type` 分区，`all` 才在同一事务内替换两类。增加回归测试证明单类型运行不改变另一分区。

### B3【阻断】判定缓存无法响应提示词/模型变更，失败结果还可能被永久固化

- 缓存主键只有 opp 对与两个 `problem_mode` 哈希：`pipeline/docs/施工任务书_战略视图.md:102-112`。
- 运维说明却要求提示词变更后手动重跑：同文件 `:275-276`。按现有键，旧对全部命中，新的判定提示词不会生效；`model` 虽落列，也不参与命中条件。
- 判定失败按 `same=False` 处理：同文件 `:147-148`，但 schema 没有“失败/可重试”状态。若实现把该默认 false 写入缓存，瞬时故障会永久伪装成真实的 `same=false`。

缓存键或命中条件必须纳入明确的 judge prompt/policy 版本（必要时含模型版本）；重试耗尽的失败不得作为正常 false 缓存，或必须有可重试状态。冻结决议 5 可以保留为“本轮不合并”，不能变成“永久判不同”。

### B4【阻断】“镜像母本”与测试/台账要求对召回对的处理数量互相矛盾

真实母本会跳过已经并入其他 leader 的 `i/j`，只对当时仍未归并的 pair 调 LLM：`pipeline/voc_analytics/stages/stage1.py:414-431`。任务书却同时要求：

- 结构只比母本多三项差异，没有写“先判完全部召回对”：`pipeline/docs/施工任务书_战略视图.md:140-145`；
- 第二轮 `cache_hits=召回对数`：同文件 `:285`；
- 验收 `llm_judged + cache_hits = 实际召回对数`：同文件 `:303`；
- 成本估算也按约 4 千个召回对全部做短判定：同文件 `:359-360`。

例：A-B 与 B-C 都被召回；若 A 先吸收 B，母本会跳过 B-C，因此“判定数=召回对数”不成立。任务书必须二选一。按现有测试、台账和成本口径，最小改法是明确“先对全部去重后的召回对查缓存/判定，再按固定 leader 顺序只消费判定结果聚类”；若坚持母本的惰性判定，则测试 4、验收等式、对数指标和成本口径都要改成“实际评估对数”。

## 三、规格可执行性：建议与存疑

1. 【建议】交付物写“新增 4 个查询”，正文只命名 `STRATEGY_AXES`、`STRATEGY_AXIS`、`STRATEGY_AXIS_MEMBERS` 三个，第四项实际是修改既有 `SHELL_COUNTS`：`pipeline/docs/施工任务书_战略视图.md:36,254-262`。若两 tab 的 N/M 需要独立计数查询，应明确命名；否则把数量改成“三个新增 + 一个既有查询改造”。

2. 【建议】`axis_name≤24` 与命名回退不闭合。回退值是 leader 的 `problem_mode` 原文：`pipeline/docs/施工任务书_战略视图.md:164-167`；现有 problem_mode 契约允许 15–40 字：`pipeline/voc_analytics/prompts.py:186-194`，而 `check_problem_mode()` 只拦空值、泛化名和少于 8 字，不拦超过 24 字：`pipeline/voc_analytics/stages/validate.py:376-386`。需要规定回退时如何满足 24 字契约，或承认 24 字只约束成功的 LLM 响应，并不要把它写成列不变量。

3. 【建议】任务书说 §9 四个误合实例都必须进入提示词和测试 fixture：`pipeline/docs/施工任务书_战略视图.md:351-357`，但 §3.4 只明确给出其中第一例，§6 测试 3 也只测第一例：同文件 `:152-167,283-284`。应把四例做参数化 fixture，并加静态断言确保它们确实进入对应判定模板。fake 返回 false 只能证明代码服从 fake，不能证明提示词带了红线。

4. 【建议】v2 的 `link_spu` 在多个 SPU 的 `evi_count` 并列最大时没有 tie-break：`pipeline/docs/施工任务书_战略视图.md:247-251`。补 `evi_count DESC, spu ASC` 即可保证复跑稳定。

5. 【建议】`top_spus`、`categories`、`prod_lines` 都应写明稳定排序（证据数相同时按 SPU，数组按文本），否则同一数据的 JSON/数组顺序可能漂移。它不影响 axis_id，但会让展示、提示词输入和快照对账出现无意义变化。

6. 【建议】新增详情路由后，侧栏当前只在 `current_path == '/strategy'` 时高亮：`system/app/templates/base.html:33-35,49-50`。如要求详情页仍高亮“战略视图”，应把 `base.html` 列入交付物并覆盖 `/strategy/axis/`。

7. 【建议】双层扩散条不是现有 `.bar` 的直接能力；现有 CSS 只有一个 `::before` 宽度：`system/app/static/app.css:169-184`。任务书应允许按需要改 `app.css`/viewmodel，而不是把交付物清单写得像只能改两个战略模板与路由。

8. 【存疑】任务书 §9 的卡数、覆盖数、配对数及示例轴来自生产探针；本次硬边界禁止连库，无法独立复测。它们与已读文件没有除 F3、F6、F8 外的直接矛盾，故本报告不质疑其真实性；验收方仍应保留原探针产物作为数字来源证据。

## 四、与 v3 的冲突面及“零改动”结论

结论是：**数据架构层可以做到与 v3 解耦，但当前任务书不能称为对 v3 完全零改动，也尚未做到零冲突。**

已核实相容的部分：

- 【建议，无需改设计】034 新增独立表，不改 030–033，也不要求写回 `voc_opportunity.n_eff/scope/scope_source`；符合 v3 将战略视图另立管道的边界：`pipeline/docs/架构规格_v3.md:227-240,350-360`。
- 【建议，无需改设计】老品扩散只读 `voc_spu_issue` 兼容名，不重新展开消息数组，符合 v3 单一归属与兼容视图设计：`pipeline/docs/架构规格_v3.md:303-345`。
- 【建议，无需改设计】保留旧列、系统层停止读取旧 scope，符合 v3 对退役列的处置：`pipeline/docs/架构规格_v3.md:354-360`。

必须修正后才相容的部分：

- 【阻断】战略输入须遵守 v3 shadow 的代次隔离，见 F5；否则直接破坏 `施工任务书_v3.md:182-184` 的硬边界。
- 【阻断】可选挂钩必须落在真实统一收尾入口 `run_generate.py`，见 F2。它是新增派生步骤，不改变 G1–G5 或 Stage1–4，但确实是对 v3 调度代码的增量修改。
- 【阻断】战略失败不改变主管线退出码可以保留，但必须单独写一条 failed strategy run log；不能把 finalize 标成失败，也不能吞掉无台账异常。现有 finalize 成功/失败日志统一在 `pipeline/scripts/run_generate.py:246-281` 收口，任务书需要明确插入点和独立 RunCtx/run-log identity。

## 五、§6 十条测试逐项审查

| # | 判定 | 它现在能挡住什么 / 挡不住什么 |
|---:|---|---|
| 1 | 【建议】覆盖不足 | 同一有序列表跑两次只会证明同一执行路径重复。第二次应打乱 DB 返回/输入顺序，才能挡住忘记 `(evi_total DESC, opp_id ASC)` 排序。还应固定 fake verdict，避免把缓存命中误当算法确定性。 |
| 2 | 【建议】基本有效 | 能挡传递闭包，但 fixture 需同时明确 A-B、B-C 的 `same=true` 和 leader 顺序。它不能解决 B4 的“全量预判还是惰性判定”歧义。 |
| 3 | 【建议】名义覆盖 | fake 对指定 pair 返回 false 只测通用 false 分支，不测红线是否进入 prompt。且与 §9 要求的四个 fixture 不一致。按四例参数化并静态检查模板。 |
| 4 | 【阻断】与母本冲突 | 只有“所有召回对都先判定/查缓存”时，第二轮 `cache_hits=召回对数` 才成立；镜像母本的惰性流程不成立。先修 B4。 |
| 5 | 【建议】只覆盖一种失效 | 能挡 mode hash 漏用，但挡不住提示词/模型版本变更继续命中旧缓存，也没挡“失败 false 被缓存”。补 policy-version 与失败后下轮重判。 |
| 6 | 【建议】条件有效 | “无写入”必须同时断言 axis、member、pair cache 均未变，命名 LLM 未调用；只检查轴表不够。退出码应在 `run_strategy.py` 入口层测。 |
| 7 | 【建议】覆盖不足 | 能挡单卡和 `n_spu=1`，但还应覆盖诉求 `n_cards=1`、缺少 `voc_spu_issue` 行的成员贡献 0/无链接、distinct SPU 去重，以及单类型重算不删除另一类型。 |
| 8 | 【阻断】期望值错误 | 90/10 应约 1.2195，见 F3。修正后，再让同一 SPU 的多张成员卡先求和，可挡“把卡当 SPU”回归。 |
| 9 | 【阻断】没有测到事务 | 命名发生在落库事务之前；命名抛错时旧数据完好是自然结果，不能证明 DELETE/INSERT 回滚。应在 DELETE 之后或插入部分 axis/member 后注入异常，再断言旧两表逐行完好。 |
| 10 | 【建议】部分有效 | 能挡泛化名二次失败后的回退分支，但没挡回退值超过 24 字，也没证明共用命名模板返回的 `summary` 长度/NULL 规则。先闭合长度契约。 |

system smoke 的补充判定：

- 【阻断】现有要求只测 `/strategy/axis/{id}` 自身 200/404，不会点击成员链接，因此挡不住 F1。应渲染一条老品、一条新品成员并实际 GET 生成的 href。
- 【建议】增加 tab 计数、排序白名单/默认顺序、双层 bar 两个宽度、top SPU 只展示前 2 个、轴层无状态标记、空状态文案，以及模板中无固定描述性散文的契约测试。
- 【建议】迁移静态测试应覆盖可重跑写法、三角色 GRANT、成员类型约束和必要索引；当前 fake 路由测试不能替代这些检查。

## 六、§8 冻结决议复核

1. 【建议，无实质异议】贪心 leader 替代传递闭包有任务书内的明确反例与规模证据。需要修的是 B4 的判定时序，不是改算法选型。
2. 【建议】0.75/TOPK 15/MAX 20000 可作为起步参数；本审查不重做生产标定。只需修正 F6 的理论上界注释。
3. 【建议，无实质异议】无人工状态、无历史表、内容寻址 ID 与“纯派生读模型”一致。单类型重算必须按 B2 做分区替换，才不会误删另一类。
4. 【建议，无实质异议】扩散面取兼容视图符合 v3 归属契约。但战略 `n_eff` 的权重是通过成卡门槛后的 `evi_count`；026 的旧 `n_eff` 实际按线程封顶且明确不套成卡门槛：`pipeline/sql/026_spu_layer_inherited.sql:23-31,104-129`。两者数学表达式相同，统计口径不同，任务书不应让实现者误以为要复刻 026 全口径。
5. 【阻断】“失败本轮按不合并”合理；若失败写入正常 false 缓存则有实质错误，按 B3 修正后可冻结。
6. 【建议，无实质异议】两类分开聚类正确，也与两套提示词/准入门一致。
7. 【建议，无实质异议】旧列保留、系统停读与 v3 迁移策略一致。
8. 【阻断】战略失败不得影响主管线退出码可以保留，但真实挂钩、独立失败台账必须按 F2 和第四节闭合。
9. 【建议，无实质异议】`n_spu` 只取带 `>=2` 独立消息门的兼容视图，是明确的新战略口径；不要把它表述成 026 旧 `n_eff` 的完整复刻。
10. 【建议，无实质异议】不展示品线/品类和轴级状态与现有数据空度、状态归属一致，也符合“界面不得硬编码描述性散文”的全局规范。任务书指定的轴名、summary 是管线数据，空表句是状态反馈，均不构成违规。

## 七、最小修订清单

修完以下内容即可派发，不需要扩大范围：

1. 改正两类详情 URL；smoke 实际跟随成员链接。
2. 把挂钩与 `--skip-strategy` 的施工点改为 `scripts/run_generate.py`，并定义独立 strategy 失败台账。
3. 为战略输入冻结发布代次；禁止 shadow 期间混合 `OPP-*`/`OPP2-*`。
4. 034 补幂等策略、角色 GRANT，并让成员 `axis_type` 与父轴一致。
5. 明确 `--type` 是分区替换，`all` 才同时替换两类。
6. 冻结“全部召回对先判定，再做 leader 聚类”或同步改掉所有按全召回对计数的测试/台账；不要保留两套口径。
7. 缓存加入判定策略版本；失败不落成正常 false 缓存。
8. 把 90/10 期望改为约 1.22，并把事务测试的故障点移到 DELETE/INSERT 事务内部。
9. 把交付物改成 3 个 prompt 模板、3 个新增查询 + 1 个既有计数改造（或明确第四个查询），并闭合 prompt 四反例、名称长度、`prod_line='未定'` 和 tie-break 规则。

完成上述修订后，任务书的核心方案具备可施工性；无需改变贪心 leader、0.75/TOPK 15、兼容视图取扩散面、纯派生全量重建或界面信息架构这些已冻结取舍。
