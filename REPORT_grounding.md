# Stage 4 grounding 自查报告

## 改动定位

以下行号以本次交付后的文件为准。

| 缺陷 | 修改位置 | 处理结果 |
|---|---|---|
| D1 `check_orphan_claims` 只看现象段 | `pipeline/voc_analytics/stages/validate.py:170-214` | 保留原函数并扩到 `title`、`problem_mode`、`desc_phenomenon`；错误逐条带字段名。没有新增平行的机型/数值校验函数。 |
| D2 缺 `content` / `content_zh` | `validate.py:137-149`、`170-192`、`332-336` | `_evidence_parts` 统一提供 `snippet + content + content_zh` 去重并集；`check_orphan_claims` 再并入 citation `value/unit` 与 `product_name`；`_evidence_corpus` 复用同一口径。 |
| D3 `_NUM_PAT` 单位不全、裸数字比对 | `validate.py:27-80`、`93-134` | 扩全任务书单位；新增 `_num_tokens`，用 `Decimal` 生成 `(归一数值, 量纲)` token，不再用数字字符串包含关系。 |
| D4 无品名组主体校验空转 | `validate.py:431-518` | 从证据正文的“命名片段 + 产品词”结构动态抽品牌候选；排除 `BRAND_OWN_ALIASES`，并检查标题点名的第三方 token 或仅在第三方上下文同现的型号。代码没有竞品名单。 |
| D5 无极性校验 | `validate.py:389-413` | 新增 `check_polarity`；只有结构化 `sentiment` 完整、非负面比例达到配置阈值且无 `low_conf` 才判断缺口断言词。阈值和词表位于 `config.py:118-124`。 |
| D6 无短证据约束 | `validate.py:521-596` | 新增 `check_short_evidence`；低信息证据仅检查 `title/problem_mode` 新增的具体实体。数值/型号仍单点委托既有 `check_orphan_claims`，避免平行闸门。阈值位于 `config.py:114-117`。 |

接入与运行模式：

- `validate.py:612-659` 在 `validate_stage2` 里先跑既有结构/逐字溯源，再跑 grounding。原三参数调用仍兼容。
- `config.py:109-113` 新增 `GROUNDING_ENFORCE`，默认环境值为 `0`，即 `False`。
- `generate.py:77-116` 报告模式只计数；强制模式把错误带入最多三次重试，三次仍失败时抛入外层既有 `failed_groups` 作废路径。
- `pipeline.py:192-196` 初始化 grounding 指标；`pipeline.py:495-505` 汇总；`pipeline.py:570-588` 校验观测计数单调性，并要求 `grounding_rejected_groups <= failed_groups`，没有新建平行终态。
- `prompts.py:86-87`、`145-147`、`216-217` 最小扩写禁止项并加入 C10 忠实转述正例；因提示词内容变化，将 `VER` 从 `v1.2.0` 升为 `v1.2.1`。
- `MIN_EVIDENCE`、`GENERATION_MAX_FAILED_RATIO`、`GENERATION_MIN_FAILED_ABS` 均未改动。

## 单位归一

`_NUM_PAT` 先抽取十进制或分数和显式单位，`_num_match_token` 再用 `Decimal` 乘换算因子。长度归一到 mm、质量到 g、功率到 W、体积到 ml、时间（周/天/小时/分钟/秒）到秒；其余单位使用独立量纲。已验证：

- `1.8m`、`1.8米`、`180cm` → `("1800", "length_mm")`
- `1/4英寸`、`1/4"`、`6.35mm` → `("6.35", "length_mm")`
- `20L`、`20升` → `("20000", "volume_ml")`
- P2/P3 的 `1/4ネジ`、`1/4螺口` 按行业省略写法归到 1/4 英寸，只在数字紧邻这些词时生效。

已知盲区：

- 只识别显式阿拉伯数字 + 单位；裸数字、中文数词、科学计数法和只在区间末尾写一次单位的前端点不会全部捕获。
- `度` 保留为通用 degree，未与 `℃` 等价，以免把 P2 的 `360度相机` 当温度；这会漏掉“35度”与“35℃”的等价关系。
- 月和年没有互换，避免引入月长/闰年的业务假设。
- 大小写宽松匹配会让孤立小写 `k` 按色温 K 处理；`Nm` 也不区分纳米写法 `nm`。当前语料的目标单位里没有纳米，但这是已知歧义。
- 葡语 `4 meses` 等外语单位不在表内；若产出也沿用外语单位，当前不会做跨语种单位翻译。

## C 组实际结果

以下均使用任务书原文与原产出，在 `test_grounding.py:38-116` 逐条执行；“命中”是实际返回的规则，不是预期描述。

| 样例 | 实际结果 | 实际命中 |
|---|---|---|
| C1 | 报错 | `check_orphan_claims` 拦无据 `3秒`；`check_polarity` 拦正面证据上的“过长”。产品编码 3318 不再为 3 秒提供假来源。 |
| C2 | 报错 | orphan 拦无据 `15cm`；polarity 拦正面证据上的“过大”。证据中的 34cm/20cm 不会为 15cm 放行。 |
| C3 | 报错 | 短证据拦未出现的“实时取景/频闪干扰”具体化。 |
| C4 | 报错 | 短证据拦“磁吸阵列中心对齐”等新增结构实体。该原产出标题没有点名“阅星瞳”，所以本条实际不是由第三方 token 分支命中。 |
| C5 | 报错 | 100米本身有来源；短证据拦证据中不存在的“灯光品类”跨品类主体。 |
| C6 | 报错 | 短证据拦“扣分系统”和“扣分底座”，没有把评价词的字面出现误当部件来源。 |
| C7 | 报错 | 短证据拦括号中凭空增加的“易刮花、掉漆”。 |
| C8 | 报错 | 短证据拦括号中凭空增加的“色温/亮度调节模块”。 |
| C9 | 报错 | orphan 放行证据已有的 40W，单独拦无据 50W、60W。 |
| C10 | 报错 | 短证据拦“三轴云台”“防滑脚垫”“支撑系统”等新增实体。 |
| C11 | 报错 | 短证据拦“热插拔/双电协同”具体方案。 |
| C12 | 报错 | orphan 拦无据 `35℃`。 |

## P 组实际结果

以下均在 `test_grounding.py:119-155` 逐条执行，四项 grounding 检查实际都返回空列表。

| 样例 | 实际结果 | 放行依据 |
|---|---|---|
| P1 | 放行 | P4P 与保护壳均来自原文；含数字的 P4P 被识别为型号而非第三方品牌。 |
| P2 | 放行 | `1/4ネジ` 与 `1/4英寸` 数值归一相同；日文到中文不做实体裸字面否决。 |
| P3 | 放行 | `1/4螺口` 与 `1/4英寸` 归一相同，“螺纹接口/螺口”按接口同义写法放行。 |
| P4 | 放行 | `iPhoneのpro max` 仅忽略日语属格连接词后匹配 `iPhone Pro Max`；括号内容可溯源。 |
| P5 | 放行 | `20L` 数值和“灰色版本”、F08 均在证据中。 |
| P6 | 放行 | 日语“明るさ/点滅”到中文“亮度/闪烁”不因字面不同触发短证据错误。 |

另有独立跨语种回归：葡语笼统故障忠实转述成中文时放行；C7/C8 仍因括号新增具体实体而报错。

## 报告模式、强制模式与账本

- 报告模式实测：一次输出命中 2 个 grounding issue，`grounding_checked_attempts=1`、`grounding_flagged_attempts=1`、`grounding_issue_count=2`、`grounding_rejected_groups=0`，返回 `_needs_review=False`，没有重试或作废。
- 强制模式实测：同一桩输出调用三次，计 `checked=3`、`enforced=3`、`flagged=3`、`issues=6`，最终 `grounding_rejected_groups=1` 并抛 `LLMError`，由外层计入既有 `failed_groups`。
- 账本实测：`planned_groups=1 = completed_groups(0) + failed_groups(1) + cancelled_groups(0)`；`grounding_rejected_groups(1) <= failed_groups(1)`，`generation_reconciliation.complete=True`。

## 测试结果

- `cd pipeline && python3 -m pytest tests/ -q`：退出码 0；收集并通过 168 个 pytest 用例（既有 106 + 本次新增 62）。
- `cd pipeline && python3 tests/validate_quotes.py`：`PASS=31 FAIL=0`。
- 全部新增测试只使用内存对象和桩；没有连接数据库、网络、LLM 或 embedding。

## 主动调整与理由

- 增加 `BRAND_OWN_ALIASES = (VIJIM, ULANZI, 优篮子)`，否则动态抽取会把同一自有品牌的中文/英文写法判成第三方。
- 对 P4 只增加一个窄范围型号归一：忽略 iPhone 与 Pro Max 之间的日语属格 `の`。没有做通用机器翻译或跨语种语义词表。
- 去重证据三字段后再计算短证据长度，避免同一正文同时出现在 `snippet/content` 时被重复计长而绕过短证据规则。
- grounding 指标按“尝试”计数而非只按最终机会点计数，因为强制模式的三轮输出可能逐轮变化；最终作废另用 `grounding_rejected_groups` 计数。

## 仍可能误判或漏判的地方

- 第三方品牌抽取依赖品牌/产品名紧邻产品词（如“某品牌电池”“某品牌的灯”）。品牌与产品词相隔很远、只有昵称、或标题用泛化品类时可能漏判；没有品名且没有可识别命名结构时不会猜品牌。
- 短证据实体抽取是受控后缀和括号规则，不是中文分词器。新型部件名若不含当前后缀、且不在括号中，可能漏过；反之，证据用同义词而没有 `content_zh` 时可能报错。
- 为保护 P2/P6，纯外语证据没有 `content_zh` 时不做中文实体裸字面比较，只检查明确的括号扩写。因此 C7 一类如果把无关发明移出括号，存在漏判可能。
- 极性规则完全依赖结构化 `sentiment/low_conf`。字段缺失时主动不判；上游极性本身标错时仍可能误报。词表外的反转表达也会漏过。
- 短证据阈值 80 是按这批已确认的 10–67 字低信息样例取的报告模式初值，不代表已完成线上标定；启用 `GROUNDING_ENFORCE=True` 前仍应按报告指标抽样校准。

---

# 附：真库标定与两处修正（2026-08-17，验收方补记）

以上为 Codex 第一轮交付的自查。验收方随后把该校验器拿到**它未见过的真库
3934 条机会点**上复算（数据由旧提示词 v1.2.0 产出），结论与两处修正如下。

## 首轮实测：命中 47.3%，不可开强制

| 规则 | 命中 | 占比 |
|---|---:|---:|
| polarity | 1062 | 27.0% |
| short_evidence | 1015 | 25.8% |
| orphan(title/mode) | 249 | 6.3% |
| title_subject(无品名) | 145 | 3.7% |
| 至少命中一条 | 1862 | **47.3%** |

`GENERATION_MAX_FAILED_RATIO=0.2`，故当时开强制会让每轮立刻中止。
召回可用：验收方独立判定的 189 条无据条目命中 173 条（91.5%）。

## 修正一：极性口径（`config.POLARITY_NONNEGATIVE`）

1062 条 polarity 命中里 769 条（72%）的证据极性**全为「中性」**，其中 610 条来自
需求缺口渠道。用户问「什么时候出 X」情绪上就是中性，但那正是真实诉求缺口——
把中性等同于「用户已满意」是口径错误。收紧为只认「正面」后降至 195 条（5.0%），
C1/C2 不受影响（两者证据均标注为正面）。

## 修正二：短证据的两处「罚格式」（`validate._title_body` 与 `_ENTITY_SUFFIX_PREFIX`）

- 1015 条命中里 658 条（64.8%）罚的词来自**标题主体前缀**（如「摄影手柄品类：」
  里的「手柄品类」），而「主体：动作」是 `_TITLE_RE` 与提示词强制要求的格式。
  标题改为只扫冒号之后。
- 抽象后缀继续在罚句式：「能力」217 次、「接口」201 次、「功能」70 次，而
  「新增 X 能力」正是 `prompts.ACTIONS` 规定的标准句式；同时「时因接口」
  「在无功能」「理与协议」这类从词中间切出的碎片也来自同一机制。
  只保留具体部件后缀（系统/模块/组件/底座/云台/脚垫/协议/阵列）。
  C 组无一因此丢失：C6 仍由「扣分系统」「扣分底座」命中，
  C3/C4/C7/C8/C10/C11 由括号扩写通道命中。

**代价：C5 检测丢失**，已标为 `xfail(strict=True)` 并在用例内写明完整原因，
不删除、不隐藏。原因是其唯一命中来自被罚的主体前缀，而「100米」在证据中确有
来源、标题也未点名「小隼」；要真正拦住这类跨品类误归需要产品→品类的结构化
映射（社媒无 `product_name` 时不可得），字符串规则无法把它与 P1
「摄影灯配件品类：推出 P4P 专用保护壳」区分开——后者品类同样不在证据字面里，
却是正确的归类推断。

## 修正后实测

| 规则 | 命中 | 占比 |
|---|---:|---:|
| short_evidence | 778 | 19.8% |
| orphan(title/mode) | 249 | 6.3% |
| polarity | 195 | 5.0% |
| title_subject(无品名) | 145 | 3.7% |
| 至少命中一条 | 1269 | **32.3%** |

召回 85.7%。测试 167 passed + 1 xfailed + 19 subtests；C 组 11/12、P 组 6/6。

两处 `grounding_issue_count` 的精确值断言改为下界断言：这两个用例验的是
「报告模式只计数不作废 / 强制模式重试三次再走既有作废路径」这一行为，
规则覆盖面由 C 组与 P 组用例负责，不应把规则集细节钉死在模式测试里。

## 残余 32.3% 是什么：抽样人读

验收方抽 12 条「被命中、但数字/型号判据看不见」的条目逐条人读：
约 9 条为真命中（如 10 字证据「为什么不出续航手柄啊」→「热插拔+双电协同」；
6 字「无法调节水平」→「微调旋钮+气泡水准仪」；polarity 正确抓出「整体高度可以
调节」那条语义反转），约 3 条为误杀（「没有直接架相机的」→「无需额外转接件」
属忠实转述）。**精确率约 75%**。

因此 32.3% 并非误杀堆积，而主要是真实缺陷——与 Gate B 手读「45% 的单证据
机会点有实质违例」一致。**结论：闸门够用，超阈值的原因是旧提示词下生成器
本身的缺陷率就这么高。** 而本批数据全部由 v1.2.0 产出，提示词已升至 v1.2.1
正是要从源头压低它，故**继续在旧数据上标定强制阈值没有意义**；应带
`GROUNDING_ENFORCE=False` 重跑一轮，用新提示词产出的报告模式计数作为
开强制的依据。
