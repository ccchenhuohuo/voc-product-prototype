# VOC v3 数据架构只读审核报告

审核日期：2026-08-20  
审核结论：**NO-GO，不建议按现状直接执行并放行全量刷新。**

## 0. 审核边界与结论摘要

本报告是静态只读审核。按照委托边界，本次没有连接数据库、没有执行 SQL、没有运行测试或脚本、没有调用 LLM/embedding、没有联网，也没有修改本报告以外的文件。因此：

- “代码/DDL 能否自洽”是已核实结论；
- “真实库里是否存在脏 SPU、旧人工裁决、权限漂移、锁等待超时”等只能列为【存疑】，必须由探针证伪；
- 迁移能否在真实 v2 库一次通过，仍需隔离库或恢复副本演练。

总判断分成两层：

1. **快照冻结机制本身基本成立。**031/033 的主键、不可 UPDATE、整轮 DELETE、单写入口、内容指纹、运行角色权限，以及正式生成、关系落库、v3 MV、前端原声读取的路径是连通的。全仓没有发现 v3 派生层重新展开 `voc_message.spu` / `spu_inherited` 的违规路径。
2. **系统没有证明“冻结快照中的每个归属都到达了路由和关系层”。**当前 `R=F` 的 F 来自已经进入 Python 老品池的同一批行，不来自物理快照；收尾只拦“关系投影多于快照”，不拦“少于快照”。同时，Stage1 未分类和 group 失败被允许计为 `complete=True`，但又没有携带 `run_id + assigned_spu` 的逐行终态账。因此一次全量可以绿灯结束，却缺少冻结归属对应的关系行。

在全量刷新前至少要关闭下列三项【阻断】：

- 建立“物理快照集合 ↔ 实际路由集合”的独立集合相等门；
- 建立“物理快照集合 ↔ 关系投影（或可审计终态）”的闭合门，不能只检查上界；
- 明确应用与兼容视图的协调切换/协调回滚，不能把 `voc_spu_issue` 一句换指向当作整个前端的代次开关。

如果业主暂不改代码，最多可以在**仍由 v2 对外、且新应用未部署**的 shadow 环境跑一次候选全量，再用本报告第 8 节的外部硬门人工判定；不得因现有 `reconciliation.complete=True` 或 `probe_conservation.py` 显示 `R=F` 就切换生产。

---

## 1. 你独立核实为事实错误的地方

本节只列能够从仓库代码直接证明的错误断言或失效保护，是本报告最重要的部分。

### 1.1 【阻断】当前 `assignment_conservation` 不是“物理快照到路由”的守恒

规格把 F 定义为冻结 G5 快照的扇出行数，并要求 `R=F`（`pipeline/docs/架构规格_v3.md:368-387`）。实际代码却在完成取池、G4、G5 和生命周期筛选之后，从 `old_rows` 自己生成 `expected_assignment_keys`，再把同一批行按桶求和为 R（`pipeline/voc_analytics/pipeline.py:233-264`）。物理快照行数 `snapshot_rows` 只被写进 metrics（`:267-271`），没有参与等式。

因此，以下错误都可以让 F 与 R 一起变小并继续通过：

- 快照 JOIN 漏一行；
- 033 与 Python G4/G5 口径漂移；
- 某个快照归属在进入 `old_rows` 前被降级、过滤或丢失。

`pipeline/scripts/probe_conservation.py` 有同样问题：F 从 `old` 分组得到，R 直接等于 `len(old)`（`:73-97`）。它能发现“同一 Python 行没有被放进桶”一类局部错误，但不能独立证明快照完整进入了路由。该脚本输出 `R=F` 不能作为发布门。

相关测试只把人造 metrics 喂给对账函数：默认快照、F、R 都是 0（`pipeline/tests/test_generation_reconciliation.py:19-29`），另一个用例只证明手工设置 `F=2/R=1` 会失败（`:198-209`），没有从两个独立数据源计算 F/R。

### 1.2 【阻断】收尾投影检查的方向反了：漏投影会通过

规格要求：

```text
P >= F
count(DISTINCT (message_id, seq, assigned_spu)) = F
```

见 `pipeline/docs/架构规格_v3.md:380-386`。

`validate_assignment_projection()` 已计算当前 run 的 `projected_rows` 和 `projected_distinct_rows`（`pipeline/voc_analytics/pipeline.py:735-795`），但最终只在 `projected_distinct_rows > snapshot_rows` 时失败（`:797-806`）。它会拦截超出快照的关系，却不会拦截：

- `projected_distinct_rows < snapshot_rows`；
- `projected_rows < snapshot_rows`；
- `snapshot EXCEPT relation` 非空。

也就是说，最关键的“快照有、关系没有”恰好是允许通过的方向。函数名和收尾日志给出的“投影自检通过”不能证明规格的 P 守恒。

### 1.3 【阻断】`complete=True` 明确允许事实没有任何关系行，且终态账无法按 SPU/run 对账

Stage1 未分类的老品行只写 `voc_unclassified_evidence`，不写机会关系（`pipeline/voc_analytics/pipeline.py:345-359`）；单个生成 group 的普通失败会被跳过（`:365-405`）。最终对账明确允许 `failed_groups > 0`，仍可 `complete=True`（`:622-636`）。测试也把这件事钉成预期行为：

- grounding 拒绝 1 个 group 后仍断言 `complete is True`（`pipeline/tests/test_grounding.py:302-327`）；
- Stage1 漏归属进入 unclassified，不中止整轮（`pipeline/tests/test_stage1_accounting.py:140-155`）。

更关键的是，这个“显式终态”不能补规格等式：`voc_unclassified_evidence` 只有 `(message_id, seq, week, reason)`，主键也是 `(message_id, seq, week)`，没有 `run_id`、`assigned_spu` 或 group 身份（`pipeline/sql/001_schema.sql:70-79`；`pipeline/voc_analytics/db.py:188-190`）。同一事实扇出到多个 SPU 时，多份终态会折叠成一行；失败 group 更只有汇总计数，没有逐事实终态。

所以 BUILD 报告第 8.3 条不是过度谨慎，而是实际阻断，并且实际缺口比报告写得更大：当前既不满足严格的 `P/D=F`，也没有足够的数据结构支持“关系 + 允许终态 = F”的替代等式。

### 1.4 【阻断】`voc_spu_issue` 视图不是整个应用的 v2/v3 开关，单独切换或回滚会混代

规格声称消费方零改动、切视图即可切换/回滚（`pipeline/docs/架构规格_v3.md:318-341`）。当前应用并非只通过这个视图选择代次：

- 侧栏新品和战略数硬编码 `OPP2-%`（`system/app/queries.py:44-55`）；
- 首页两类流转统计硬编码 `OPP2-%`（`:127-140,185-198`）；
- 状态、创新、战略等查询也硬编码 OPP2（`:265-302,763-770,1070-1085`）；
- 老品列表的证据聚合要求 `oe.assigned_spu = spu`（`:665-692,851-885`）；
- 原声详情要求 `oe.assigned_spu = %s`（`:975-999`）。

031 给存量 v2 关系新增的三列均为 NULL（`pipeline/sql/031_assign_snapshot.sql:79-126`）。因此：

- **新应用 + 视图仍指 v2**：老品问题列表来自 v2，但其近期证据/详情原声为空；首页/新品/战略又读 OPP2，形成混代页面；
- **新应用 + 视图切回 v2**：只能恢复 v2 问题列表，不能恢复依赖 `assigned_spu` 的 v2 原声；
- shadow 生成期间若提前部署新应用，直接读 OPP2 的区域可能看到未收尾的部分数据，兼容视图仍指 v2 也挡不住。

这证明 BUILD 第 8.5 条成立，而且不只是“回滚不完整”，正向部署顺序同样没有由视图保护。必须把“应用版本 + 视图指向”作为同一个发布单元；回滚也必须二者一起回。

### 1.5 【建议】“两个 MV 各自刷新、互不阻塞”与实现不符

规格写两个 MV“各自独立刷新与自检，互不阻塞”，并写刷新函数“按当前指向刷新”（`pipeline/docs/架构规格_v3.md:338-345`）。实现一次顺序执行三个普通刷新：`voc_spu`、`voc_spu_issue_v2`、`voc_spu_issue_v3`（`pipeline/sql/032_spu_issue_v2v3.sql:131-143`）。

普通 `REFRESH MATERIALIZED VIEW` 不是 `CONCURRENTLY`；在同一函数/事务内，锁持续到事务结束。兼容视图指向的一代也会在刷新期间被阻塞。它保证的是事务一致性，不是“互不阻塞”。当前数据量可能使阻塞很短，但必须通过真实库锁时长探针确认，不能按规格文字假定无阻塞。

### 1.6 【建议】MCP 的“最近已发布数据”查询按官方收尾路径不会命中

`MCP_DATA_AS_OF` 要求一行同时满足 `stage='finalize'` 且 `metrics.reconciliation.complete=true`（`system/app/queries.py:1275-1285`）。官方 supervisor 用 `--finalize-only` 启动收尾（`pipeline/scripts/rerun_both.sh:438-446`）；此时新的 ctx 没有 `generation` 节点，而 `_save_log()` 只在存在 generation 时计算 reconciliation（`pipeline/scripts/run_generate.py:41-60`）。因此代码生成的 finalize 成功行不带该字段，查询不会返回它。

当前 FastAPI 页面没有使用这条 MCP 查询，所以不阻断主看板；若 MCP 面要上线，这是确定的空结果缺陷。

### 1.7 【建议】首页“账本完整”是最近一个生成子进程，不是已发布的组合运行

`HOME2_FRESHNESS` 取“最近一行带 reconciliation 的 run”，不要求 finalize 成功，也不要求老品/新品两条子账都成功（`system/app/queries.py:326-360`）。两个生命周期并行时，它可能展示某一个子进程 `complete=true`，即使另一进程失败、统一收尾从未发生。该指标不能解释为“本次发布完整”。

### 1.8 【建议】数据库没有两生命周期齐备的发布门，正确性只存在于 shell 进程状态

官方 `rerun_both.sh` 会在两个退出码都为 0 后才启动收尾，这部分是正确的（`pipeline/scripts/rerun_both.sh:431-445`）。但 `_finalize()` 本身不查询 `voc_run_log` 来证明同一 base run 的 `generate_existing`、`generate_innovation` 均成功且 reconciliation 完整（`pipeline/scripts/run_generate.py:89-203`）；CLI 又公开了 `--finalize-only`（`:215-248`）。

所以官方脚本是安全护栏，但数据库状态和收尾程序不是自证的。人工调用、调度改写或失败恢复时可以对半轮数据执行收尾。建议至少把两条子账齐备作为收尾前的硬探针；若 `--finalize-only` 可能被独立调用，则应升级为代码门。

---

## 2. 数据架构自洽性逐项审核

### 2.1 030：`voc_has_spu` 与存量重分类

结论：**基本自洽；有一个真实数据前提待证伪。**

- 迁移要求 `voc_admin`，显式锁定机会、关系、消息和来源策略表，再以统一函数重算 v2 分类（`pipeline/sql/030_has_spu.sql:9-31,33-78`）。
- 迁移内自检不是纯日志，而是非零即抛错回滚（`:80-110` 及后续 classification check）。
- 该操作会改存量 v2 的分类字段，但后续 032 用 OPP 命名空间隔离 v2/v3，路径一致。

【存疑】`voc_has_spu()` 以数组 cardinality 判非空（`:17-24`），033 则 `btrim` 后排除空 token（`pipeline/sql/033_snapshot_prepare.sql:92-108`）。如果真实库存在 `ARRAY['']` 或纯空白 SPU，030 会判“有 SPU”，033 却不会生成归属。清洗函数正常会去掉空 token并规范 SPU（`pipeline/voc_analytics/clean.py:82-95`），但迁移前存量是否全部经过这版清洗只能由探针确认。硬阈值应为 0 行。

### 2.2 031/033：快照、关系投影、触发器和权限

结论：**快照不可变与权限路径通过静态审核。**

- 快照 PK 为 `(run_id,message_id,seq,assigned_spu)`，source 受 `fact/root` 约束（`pipeline/sql/031_assign_snapshot.sql:14-27`）；同一 SPU 同时来自事实/继承时，033 确定性选择 fact（`pipeline/sql/033_snapshot_prepare.sql:164-176`）。
- UPDATE 被触发器拒绝；DELETE 必须整轮删除（`pipeline/sql/031_assign_snapshot.sql:34-77`）。
- `voc_writer` 被撤销直接 INSERT/UPDATE/TRUNCATE，只保留 SELECT 和整轮 DELETE；写入只能经 033 的 SECURITY DEFINER 函数，reader/human 只有 SELECT（`:177-185`；`pipeline/sql/033_snapshot_prepare.sql:272-276`）。
- 033 对同 run 取 advisory transaction lock；已有指纹只验证、不重建基线；“有行无指纹”和“有日志无指纹”均失败（`pipeline/sql/033_snapshot_prepare.sql:31-88`）。
- 指纹覆盖行数、唯一事实数和排序内容；收尾重新比较（`:179-215,219-268`）。
- 关系三列约束允许 v2 全 NULL，也约束 assigned/source 成对出现及 assigned 时必须有 run（`pipeline/sql/031_assign_snapshot.sql:79-126`），与并存期写法相容。

没有发现会必然造成主键冲突或正常写入被触发器误拦的静态问题。仍需在隔离库以真实角色执行 `m4_v3_snapshot.py`，因为角色成员关系、对象 owner 和历史约束状态不能从仓库证明。

### 2.3 032：双 MV、兼容视图和刷新

结论：**对象定义与命名空间隔离正确；可用性和消费面切换不完整。**

- v2 MV 只收 `OPP-*`，按消息数组展开；v3 MV 只收 `OPP2-*`，直接读关系 `assigned_spu` 并要求等于 `core_tag`（`pipeline/sql/032_spu_issue_v2v3.sql:43-110`）。两代不会在这两个 MV 内串行。
- 两个 MV 都按 `(spu,opp_id)` 分组，唯一索引不会在正常定义结果上冲突（`:112-119`）。
- 兼容名初始明确指向 v2，并重授三类运行角色 SELECT（`:121-129`）。032 迁移事务提交前外部不会看到中间“对象不存在”的状态，但会等待 DDL 锁。
- 026 的线程封顶 Simpson `n_eff`/scope 口径被完整保留给 OPP-*；032 只增加代次边界（`pipeline/sql/026_spu_layer_inherited.sql:94-150`；`pipeline/sql/032_spu_issue_v2v3.sql:160-205`）。OPP2 的 `scope_source='v3-未计算'` 行则明确清空 `n_eff/scope`（`pipeline/sql/032_spu_issue_v2v3.sql:154-158`），与本轮“不施工战略管道”的契约一致，没有用 v2 消息数组给 v3 回填共性度。
- 最近邻刷新同样按代次隔离：只删除并重建 OPP2 端，候选也只来自 OPP2 且同生命周期，v2 NN 保留（`pipeline/sql/032_spu_issue_v2v3.sql:212-250`）。
- 迁移显式取得四张基础表的 `SHARE ROW EXCLUSIVE` 锁（`:17-19`），刷新又是非并发刷新；应放维护窗口并实测锁时长。
- 切换问题见 1.4：MV 内隔离正确，不等于应用整体隔离正确。

### 2.4 运行时读写、事务和锁

结论：**单机会写入原子、锁序大体合理；全轮和收尾不是原子事务。**

- 正式 generation pool 在有 `assign_run_id` 时只从 `voc_assign_snapshot` 投影归属（`pipeline/voc_analytics/db.py:332-407`）。
- 老品桶键逐字使用 `assigned_spu`（`pipeline/voc_analytics/routing.py:56-65`）；保存关系前检查它等于机会 `core_tag` 且 run/source 完整（`pipeline/voc_analytics/pipeline.py:1132-1163`）。
- 单机会的机会行、关系行和 recount 在同一事务；机会先按稳定 ID advisory lock，再行锁（`:1055-1087,1181-1186`）。MERGE 也先稳定锁参与机会，再改关系并 recount（`pipeline/voc_analytics/execute.py:49-72,127-153`）。没有发现确定的锁序反转或必然死锁。

但：

- 每个机会、每条 split proposal、周快照、revive、MV 刷新、NN 刷新、release 都是不同数据库调用/事务（`pipeline/scripts/run_generate.py:89-203`）。后段失败会留下已提交的 OPP2、快照或提案；这是 shadow 可恢复性风险，不是全轮原子提交。
- `detect_split()` 没有“同一机会已存在 pending SPLIT”的幂等判断（`pipeline/voc_analytics/resolve.py:111-127`）；收尾失败后重试可能重复提案。
- shadow 模式明确保留既有 OPP2，不清理（`pipeline/scripts/rerun_both.sh:324-343`）。首次运行失败后的重试若 Stage1/LLM 分组变化，旧 OPP2 可能继续留在 v3 MV；当前投影校验会接受它们，只要它们仍能引用旧 run 快照。首次开跑前 OPP2 应为 0；若非 0，先停工决定失败产物处置。

【建议】官方 supervisor 已避免并行生成未结束就 finalize，但仍应以 run_log 和集合探针作为数据库侧发布门，不能只依赖进程退出码。

### 2.5 身份键与唯一约束

【存疑】机会 ID 会对 `core_tag` 和 `problem_mode` 做 NFKC、casefold，并删除多类标点/分隔符（`pipeline/voc_analytics/pipeline.py:13-42`）。碰到两个逐字不同、规范化后相同的 SPU，加上相同 problem mode，会得到同一 OPP2 ID；碰撞检查也用同一规范化函数，因而不会在保存时把它当哈希冲突（`:1055-1087`）。随后关系仍携带第二个逐字 SPU，收尾会因 `assigned_spu != canonical core_tag` 失败，但前面的写入已经提交。

正常清洗会统一空白、连字符和大小写（`pipeline/voc_analytics/clean.py:90-95`），所以这不是已证实的数据事故。全量前必须验证“规范化身份相同但 exact SPU 不同”的组合为 0；非零即停。

---

## 3. 归属契约是否真的冻结

结论：**“冻结于 run_id，v3 派生不回读消息数组”这一核心主张成立；“冻结集合被完整消费”不成立。**

### 3.1 已成立的链路

```text
033 从消息数组一次性构造快照
    ↓ run_id + fingerprint
db.generation_pool 只投影该 run 快照
    ↓ assigned_spu/source/run_id
routing 以 assigned_spu 分桶
    ↓
voc_opp_evidence 携带同一投影
    ↓
voc_spu_issue_v3 / ISSUE_VOICES 从关系投影读取
```

证据：

- 唯一展开点：`pipeline/sql/033_snapshot_prepare.sql:92-176`；
- 正式读口：`pipeline/voc_analytics/db.py:332-407`；
- 路由和落库：`pipeline/voc_analytics/routing.py:56-109`、`pipeline/voc_analytics/pipeline.py:1132-1163`；
- v3 派生：`pipeline/sql/032_spu_issue_v2v3.sql:80-110`；
- 前端问题原声：`system/app/queries.py:975-999`；
- 首页需要消息级 SPU 时也从最新已发布快照读取（`:4-30`）。

### 3.2 全仓残留逐项判断

以下数组读取不是 v3 派生违规：

- 030 的统一判断、033 的首次快照构造：冻结入口必需；
- 032 的 `_v2` 定义：灰度保留的旧代口径；
- `classification.py:40-53`：只给迁移前纯函数夹具的兼容分支；正式 v3 查询总会投影 `spu_assignments`，优先走 `:17-38`；
- `db.py:434-451` 的 `low_conf_intersection()`：数据质量诊断，不参与机会归属、分桶或 v3 派生；
- `voc_spu` 的产品容器以及前端对 `product_names/skus/brands` 的数组展开：不是证据到 SPU 的归属推导。

没有发现 `system/app/queries.py` 使用 `m.spu` / `spu_inherited` 来重建 v3 问题原声或四象限，也没有发现 `voc_spu_issue_v3` 回读消息数组。

### 3.3 没有成立的部分

冻结只证明快照内容没有变化；它不证明：

- 快照全集都进入正式 Python 老品池；
- 每个快照归属都进入某个 Stage1 桶；
- 每个快照归属最终至少挂到一张关系卡；
- 允许失败是否有逐 `run_id + assigned_spu` 的终态。

这四件事正是 1.1–1.3 的阻断项。不能把“指纹一致”替代为“消费完整”。

---

## 4. 守恒与自检能否挡住指定错误

| 要挡的错误 | 当前机制 | 判定 |
|---|---|---|
| 同 run 快照被 UPDATE/局部 DELETE/追加篡改 | trigger、权限、原指纹复算 | 能挡住运行角色的常规路径；通过 |
| 同一 `(msg,seq,SPU)` 在快照内重复 | PK + 033 `DISTINCT ON` | 能挡住；通过 |
| 已进入 `old_rows` 的同一归属重复 | `len(set(keys)) != len(old_rows)` | 能挡住；通过 |
| 快照行未进入 `old_rows` | F 不取快照集合 | **挡不住；阻断** |
| Python 路由桶漏掉已进入 `old_rows` 的行 | R 与 post-G5 F 比较 | 能挡局部漏桶，但不是端到端守恒 |
| 关系行不在任何快照中 | `missing_snapshot_rows` 反查 | 能挡住；通过 |
| 关系投影少于快照 | 只检查 `D > snapshot_rows` | **挡不住；阻断** |
| 一张老品卡混多个 SPU / 与 core_tag 不同 | 保存前校验 + finalize 全量检查 + v3 MV 条件 | 正常路径能挡；通过 |
| Stage1/group 失败造成事实无卡 | 允许 `failed_groups` 和 unclassified | **被明确放行；阻断** |
| 两生命周期只成功一个却收尾 | 官方 shell 看退出码 | 官方路径能挡，finalize 自身挡不住；建议补门 |
| 截断子集冒充全量 | `scope_truncated_* == 0` | 能挡住；通过 |

注意：`P >= F` 不是要求“每个事实只能挂一张卡”。同一 SPU 内一个事实可支持多个问题卡，所以 P 可以大于 F；真正必须相等的是关系层的 distinct 三元组集合与快照集合。当前实现缺的是这个集合等式。

---

## 5. v2 → v3 切换与并存期

### 5.1 数据层并存

数据层隔离设计是合理的：

- OPP-* / OPP2-* 分命名空间；
- `_v2` 只读 OPP-*，`_v3` 只读 OPP2-*；
- 032 提交后兼容视图仍指 `_v2`；
- v3 收尾只处理/释放 OPP2（`pipeline/scripts/run_generate.py:89-98,154-175,180-203`）。

未发现 v3 MV 把 v2 关系混入，或 v2 MV 把 OPP2 混入。

### 5.2 应用层并存

应用层不是单开关，而是三种路径并存：

1. 经 `voc_spu_issue` 选择老品代次；
2. 直接 `OPP2-%` 选择新品、首页流转、战略等数据；
3. 直接依赖 `oe.assigned_spu` 读取老品证据。

因此允许的部署矩阵实际只有：

| 应用 | 兼容视图 | 结论 |
|---|---|---|
| 旧应用 | v2 | 当前生产基线 |
| 新应用 | v3 | 目标组合，须整体验收 |
| 新应用 | v2 | **不兼容：混代且 v2 证据明细缺失** |
| 旧应用 | v3 | 仓库没有旧版本可做静态证明，不应作为发布方案 |

切换必须在维护窗口内协调：先确保 v3 全量及所有硬探针通过，再部署新应用并原子换视图；不允许长时间停在“新应用 + v2 view”。回滚必须同步回旧应用和 v2 view。

FastAPI 的 `/healthz` 刻意不访问数据库（`system/app/main.py:32-51`），应用启动也只有 `db.open()`，没有校验 030–033 对象/列和 view 指向。`queries.py` 虽定义 `MCP_SCHEMA_OBJECTS`（`:1171-1195`），仓库中没有 FastAPI 启动时调用它的路径。因此部署错配可以健康检查为绿，直到业务请求才空白或报错。切换验收不能只看 `/healthz`。

### 5.3 刷新可见性

`voc_refresh_spu_layer()` 在一个事务内依次刷新容器、v2、v3，读者不会看到“只提交一半 MV”的状态；这是优点。代价是三个非并发刷新产生的锁一起持有到提交，前端可能整体等待。没有发现刷新顺序导致 v3 依赖 v2 或反向依赖；问题是可用性而非数据顺序。

---

## 6. 对 BUILD_v3_report.md 第 8 节 7 条问题的独立裁定

| # | 施工方自报 | 独立裁定 | 严重度与理由 |
|---|---|---|---|
| 1 | 刷新规则文字冲突 | **真问题，不是过度谨慎** | 【建议】规格 `按当前指向`/`互不阻塞` 与任务书要求刷新两代冲突。实现选择刷新两代是确定且可接受的施工选择，但必须承认会锁读，并实测时长。 |
| 2 | “v2 原样改名”与 shadow 隔离冲突 | **真问题；实现选择正确** | 【建议】若不加 OPP 前缀过滤，OPP2 会被 v2 数组展开混入。032 的代次条件是必要修正，不应为了“原样”撤掉。 |
| 3 | P 放行式与允许失败不闭合 | **真问题，而且是阻断** | 【阻断】见 1.2/1.3。现有终态表还缺 `run_id/assigned_spu`，无法用替代等式闭账。 |
| 4 | `voc_opp_id_map` 无填充算法 | **真问题，首轮 shadow 非阻断** | 【建议】仓库中除 031 DDL和只读审计外没有填充路径（`pipeline/sql/031_assign_snapshot.sql:144-175`；`pipeline/scripts/audit_legacy_refs.py:18-234`）。初次 shadow 可继续保留 v2；在迁移人工状态、删除 v2 或宣称可按 map 回滚前升级为阻断。 |
| 5 | 兼容视图不足以单独回滚前端 | **真问题，而且是阻断** | 【阻断】见 1.4。不只回滚，正向部署也会混代。 |
| 6 | 备份脚本描述与仓库不符 | **真问题，但无需为 v3 改刷新逻辑** | 【建议】脚本只做 full pg_dump、人工表包与归档检查（`pipeline/scripts/voc_backup.sh:1-54`），从不刷新 `voc_spu_issue`。任务书调用点描述错误；真正要补的是恢复演练，不是给脚本硬塞刷新。 |
| 7 | 819/864 行偏差 | **事实正确，纯元数据** | 【建议】不影响架构和施工。 |

七条中没有“凭空过度谨慎”的内容；但严重度差异很大：#3/#5 是放行阻断，#4 是 v2 退役前阻断，#1/#2/#6 是文档/运维契约问题，#7 无实质影响。

### 6.1 BUILD 报告漏掉的主要问题

- 【阻断】F/R 不是从物理快照和路由两个独立集合计算；现有 probe 同样循环论证（1.1）。
- 【阻断】收尾只检查 `D <= snapshot_rows`，没有检查缺失集合（1.2）。
- 【阻断】unclassified 不能按 run/SPU 记录，failed group 没有逐行终态（1.3）。
- 【建议】finalize 自身没有验证两条生命周期子账齐备（1.8）。
- 【建议】首页 freshness 和 MCP data-as-of 对发布完成的定义不成立（1.6/1.7）。
- 【建议】应用无启动 schema/view gate，`/healthz` 不会发现错配（5.2）。
- 【建议】全轮/收尾非原子，shadow 失败重试可遗留旧 OPP2 和重复 split proposal（2.4）。
- 【存疑】脚本目标周固定为 `2026-W33`，run_id 默认也含 W33（`pipeline/scripts/rerun_both.sh:3-6,90,319,362-367,444-445`）。如果本次业主目标不是 W33，继续运行会把 `attach_week/last_week/snapshot.week` 写成错误周；必须在开跑前书面确认。
- 【存疑】规范化 SPU 身份碰撞和空白 SPU token 需要真实数据证伪（2.1/2.5）。

---

## 7. 测试能否挡住所宣称的问题

### 7.1 有价值的覆盖

- `pipeline/tests/m4_v3_snapshot.py:1-136` 是真正的数据库验收脚本，覆盖角色无 INSERT、同 run 并发准备、幂等、不同 run 隔离和指纹；但会写入/清理夹具，只能在隔离库执行。
- `pipeline/tests/test_assignment_persistence.py:41-75` 验证单次保存把三列投影写入关系。
- lifecycle/routing/identity 测试覆盖单条扇出、SPU 桶键和稳定 ID 的纯函数行为。
- `test_finalize_refreshes_spu_layer.py:33-78` 能钉住“投影校验在刷新前、刷新在 release 前”的代码顺序。
- migration tests 能发现 DDL 文本中对象或 trigger/grant 被误删。

### 7.2 不能提供的保证

- 没有测试从真实 `voc_assign_snapshot` 独立求集合，再与实际 routed keys 做双向 EXCEPT。
- 没有测试要求 `snapshot EXCEPT current-run relation = ∅`，也没有测试 `D=F/P>=F`。
- 现有测试反而明确接受 `failed_groups=1, complete=True` 和 Stage1 missing → unclassified。
- finalize 测试主要是源代码字符串顺序断言，不会发现跨事务部分提交、锁等待或重复提案。
- system SQL 测试没有覆盖“新应用 + v2 view / 新应用 + v3 view”的真实数据库矩阵，也没有验证 v2 关系三列为 NULL 时原声是否为空。
- 没有真实角色、真实对象 owner、真实行量下的迁移/刷新/锁测试；m4 只覆盖其中一部分。

因此，即使现有离线测试全绿，也不足以证明本报告的三个阻断已关闭。

---

## 8. 全量刷新前必须先跑哪些探针来证伪什么

以下按执行顺序排列。每个探针都给出硬阈值；“记录观察值”不等于放行。

### P0：先确认运行身份、目标周和干净的 v3 起点

脚本/口径：运行任何迁移或 LLM 前，读 `pipeline/scripts/rerun_both.sh` 的实际参数，并在库中盘点目标 run_id、OPP2 行、OPP2 关系和快照日志。

硬阈值：

- 业主确认目标周**确实是 `2026-W33`**；否则当前脚本不准运行；
- base `RUN_ID` 非空且不含 `:`，该 ID 及其 `:assign_snapshot/:generate_existing/:generate_innovation/:finalize` 均不存在；
- 首次 v3 shadow 开跑前 `OPP2-%` 机会、关系应为 0。若非 0，停止并判定它们是合法前跑还是失败残留；不得直接追加。

证伪：错误周、run_id 复用、失败残留污染首次基线。

### P1：迁移前旧引用和可恢复性盘点

现有脚本：

```bash
python pipeline/scripts/audit_legacy_refs.py
```

硬阈值：

- `decided_legacy_proposals == 0`；非零脚本本身返回 2，立即停工；
- 所有 `unmapped_*` 必须保存为基线。初次 shadow 可以非零，但在 v2 删除、人工状态迁移或声称 map 回滚可用前必须为 0，或有逐表书面处置；
- `voc_backup.sh` 产生的 full/human 两份归档均须有 `TABLE DATA`，并在隔离库完成一次恢复；恢复后 v2 关键表行数、`voc_spu_issue` 键集合和人工表行数与备份清单完全一致。仅 `gzip -t` 或 `pg_restore -l` 非恢复演练。

证伪：已有人工裁决无法迁移、旧引用悬空、备份看似成功但不可恢复。

### P2：真实数据清洁度与身份碰撞预检

建议做成一次只读 preflight，口径必须包含：

1. `spu` / `spu_inherited` 中 `NULLIF(btrim(token),'') IS NULL` 的数组元素数；
2. 同一清洗规范值对应多个 exact SPU 的组数；
3. 按 `pipeline._identity_text(core_tag)` 等价规则规范化后，同一规范 core_tag 对应多个 exact core_tag 的组数；
4. 同一 `(opp_type, normalized_core_tag, normalized_problem_mode)` 对应多个 exact core_tag 的候选组数。

硬阈值：四项均为 0。任何非零先修数据或明确 canonical 规则，不进入全量。

证伪：030/033 空 token 口径漂移、OPP2 身份碰撞、跨 SPU 卡在收尾才爆炸。

### P3：030–033 隔离库迁移与权限/对象验收

在生产恢复副本或专用验收库按 030→031→032→033 执行。随后运行：

```bash
python pipeline/tests/m4_v3_snapshot.py
```

注意：该脚本会写入并清理专用夹具，禁止拿生产库当第一次试验场。

硬阈值：

- 脚本退出码 0；并发两次 prepare 行数相同且大于 0；重试、verify 相同；
- writer 对 snapshot 的直接 INSERT/UPDATE/TRUNCATE 均为 false，执行 prepare/verify 为 true；reader/human 只有 SELECT；
- `voc_spu_issue_v2/v3` 的 relkind 均为 `m`，兼容名为 `v` 且定义只指向 v2；三者列名/顺序/类型完全相同；
- 两个 snapshot trigger 和 map append-only trigger 均启用；所有新增 CHECK 均 valid；差异数 0；
- 在还没有 OPP2 的恢复副本上，`voc_spu_issue_v2` 与迁移前 026 基线的 `(spu,opp_id,evi_count,msg_count,tax_path,first/last_week)` 双向差集均为 0。

证伪：真实 owner/GRANT 漂移、DDL 依赖导致迁移失败、v2 基线被 032 改坏、触发器未启用。

### P4：G4 预热与快照准备后的独立路由守恒

先按正式版本完成 G4 warm，再只准备一个全新的 run 快照。可先运行现有只读脚本：

```bash
python pipeline/scripts/probe_conservation.py --run-id "$RUN_ID"
```

但该脚本只能作为分布报告，不能单独放行。必须另加一个**临时只读对账器**，沿用其 production routing 调用，同时独立读取快照集合：

```text
S = {(message_id, seq, assigned_spu, source) from voc_assign_snapshot where run_id=RUN_ID}
R = {正式 generation_pool + G4/G5 + 老品 route 得到的同四元组}
```

硬阈值：

- `verify_assign_snapshot(RUN_ID)` 成功，日志行数/唯一事实数/指纹与表一致；
- `S EXCEPT R = 0`；
- `R EXCEPT S = 0`；
- routed 原始列表按 `(message_id,seq,assigned_spu)` 的重复数 = 0；
- `len(R) = snapshot row_count`，而不是只要求脚本内部的 `R=F`；
- `F/U` 记录并与冻结规格基线约 1.150 比较；它不是正确性等式，但相对漂移超过 5% 时先解释数据范围/规则变化再继续。

证伪：快照 JOIN 漏行、033/Python 口径漂移、分类/降级把快照归属吞掉、重复路由。

### P4b：SPU 分桶质量探针（不能替代守恒探针）

如业主还要验证真实 SPU 桶的 Stage1 可分性，再运行：

```bash
python pipeline/scripts/probe_spu_bucketing.py \
  --run-id "$RUN_ID" --limit-buckets 0 \
  --out /tmp/v3_spu_bucketing.json
```

该脚本会调用 Stage1/merge 的 LLM（`pipeline/scripts/probe_spu_bucketing.py:86-90,143-155`）并写 JSON 文件（`:194-203`），只是“不写数据库”，不能称为无成本只读探针。本次审核没有执行它。

硬阈值：`buckets_failed=0`；在当前严格 P 规格下，老品 `unclassified=0`、`dropped=0`，任何非零都预示 P5 会有快照归属无关系。卡数、singleton rate、耗时和 token 仅作容量/质量观察，不是守恒证明。

证伪：真实大桶无法完成 Stage1、老品证据大量进入无关系终态。它不能证伪 snapshot→route 漏行，也不能证伪 Stage2/3/4 或关系落库失败。

### P5：两生命周期生成后、统一收尾前的关系守恒

首次 v3 全量应在这里设置人工停点，不让 supervisor 直接越过到 finalize。检查同一 base run 的两条子账和关系集合。

定义：

```text
F = count(*) of snapshot for RUN_ID
P = count(*) of OPP2 老品关系 where assign_run_id=RUN_ID
D = count distinct(message_id,seq,assigned_spu) of those relations
MISSING = snapshot four元组 EXCEPT current-run relation four元组
EXTRA   = current-run relation four元组 EXCEPT snapshot four元组
```

硬阈值：

- 恰有一条 `${RUN_ID}:generate_existing` 和一条 `${RUN_ID}:generate_innovation`，均 `status=success`、`reconciliation.complete=true`、所有 cancelled/failed persistence 为 0、所有 scope truncation 为 0；
- 按当前冻结规格，老品 `failed_groups=0`，且 `MISSING=0`。若业主要允许失败，必须先有带 run/SPU 的逐行终态并验证与关系集合互斥、并集等于快照；当前表结构不满足，不能口头豁免；
- `P >= F`、`D = F`、`MISSING = 0`、`EXTRA = 0`；
- 所有 OPP2 老品关系 `assigned_spu/source/assign_run_id` 完整，source 与快照一致；
- 每张有效 OPP2 老品卡 `count(DISTINCT assigned_spu)=1` 且该值逐字等于 `core_tag`；跨 SPU 行数 0；
- OPP2 新品关系的 `assigned_spu/assignment_source` 均 NULL，`assign_run_id=RUN_ID`；
- 所有 OPP2 关系引用的快照 run 仍存在，历史 orphan 数 0。

证伪：关系漏写、重复分配、跨 SPU 卡、只成功半个生命周期、把允许失败伪装成完整。

### P6：收尾与派生层对账

只有 P5 全绿后才允许 finalize。收尾后独立复算：

- `${RUN_ID}:finalize` 恰一条、status success；
- assignment 指纹不变；
- v3 MV 与 OPP2 老品关系按 032 定义（包括 `HAVING count(DISTINCT message_id)>=2`）聚合后的双向差集为 0，所有数值列逐项相等；
- v2 MV 与 P3 的 v2 基线双向差集为 0；
- `msg_count < 2` 为 0；
- old snapshot denominator 的 `base_total<=0`、`neg_total>base_total`、`denominator_scope!='assigned_spu'` 均为 0；
- NN 两端 orphan 为 0；
- finalize 前后 v2 OPP-* 机会、关系、人工表、快照的行数和内容校验和不变。

证伪：MV 刷错代、刷新顺序读旧数据、v3 分母错误、NN 悬空、shadow 改写 v2。

### P7：锁和切换演练

在恢复副本按生产并发量运行 `voc_refresh_spu_layer()`，同时持续查询首页、产品列表和已知问题详情。

硬阈值：

- 演练设置 5 秒 lock timeout；任何 DDL/refresh 锁等待超过 5 秒即停止并安排更明确维护窗口；
- 刷新期间业务请求 5xx/SQL timeout 为 0；请求 p95 不超过无刷新基线 2 倍，否则不得无维护窗口刷新；
- 使用一个已知 v2 `(spu,opp)` 和一个已知 v3 `(spu,opp)` 做部署矩阵：目标组合下列表计数与关系聚合一致，详情 voice keys 与该 SPU 的关系键集合完全相等，跨 SPU voice 为 0；
- `/`、`/iter`、`/spu/{spu}`、`/issue/{spu}/{opp_id}`、`/inno`、`/strategy` 全部 0 个 5xx；不能用 `/healthz` 代替；
- 切换只允许从“旧应用+v2”直接进入“新应用+v3”；回滚演练必须同时回“旧应用+v2”。中间不接受“新应用+v2”作为稳定态。

证伪：非并发 MV 刷新导致不可接受阻塞、应用/view 错配、原声跨 SPU 或回滚空白。

### P8：发布后观察门

切换后至少核对：

- 首页显示的 generation 必须关联到同一 base run 的两条成功子账和 finalize；不能仅使用当前 `HOME2_FRESHNESS` 的单行 complete；
- MCP 若启用，`MCP_DATA_AS_OF` 必须返回本次 finalize；当前代码预期为空，未修前不得把它当 freshness 来源；
- 已知 SPU 的问题卡数、evi_count、voice 数与 P6 基线一致；差异阈值 0；
- v2 view 可随时重指，但只有应用也同步回退时才算有效回滚。

---

## 9. 最终放行判定

### 现在可以做

- 在隔离恢复库应用 030–033 并跑 P1–P4；
- 在确保旧应用继续服务、兼容视图仍指 v2 的前提下准备 shadow 数据；
- 用独立集合探针评估真实数据事实。

### 现在不能做

- 不能把现有 `assignment_conservation=True` 当作快照守恒证明；
- 不能把 `validate_assignment_projection()` 通过当作 `P/D=F` 证明；
- 不能在存在 old unclassified/failed group 时仍按冻结规格放行；
- 不能只换 `voc_spu_issue` 视图、不协调应用版本；
- 不能仅凭 `/healthz`、首页单子账 complete 或现有 MCP data-as-of 判断发布成功。

### 从 NO-GO 变为 GO 的最小条件

1. P4 的物理快照与实际路由四元组双向差集为 0；
2. P5 的 `P>=F`、`D=F`、MISSING/EXTRA/跨 SPU 均为 0；若允许失败，先补齐可逐 run/SPU 对账的终态契约；
3. 同一 base run 的 existing、innovation、finalize 三段状态全部可被数据库探针证明；
4. P6 的 v2 不变、v3 MV 精确重算、分母与 NN 自检全绿；
5. P7 的“新应用+v3 view”切换及“旧应用+v2 view”回滚均演练通过；
6. 业主确认目标周确为脚本硬编码的 `2026-W33`，或在运行前用正确周参数替代。

在这些条件满足前，结论保持：**可以继续做隔离探针，不可以把全量结果视为可安全切换的生产 v3。**
